# Server
PORT=3000

# Twilio: one set per client. Add more blocks for each clientName used in the API.
# Pattern: {CLIENT}_ACCOUNT_SID, {CLIENT}_AUTH_TOKEN, {CLIENT}_VERIFY_SERVICE_ID
# Example for client "ISES":
ISES_ACCOUNT_SID=your_twilio_account_sid
ISES_AUTH_TOKEN=your_twilio_auth_token
ISES_VERIFY_SERVICE_ID=your_twilio_verify_service_sid

# Add more clients as needed (NUHOME, BoilerBase, OPTECGROUP, etc.)
